# Node modules
node_modules/

# Output directories and files
dist/
build/
out/

# Log files
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Env variables
.env
.env.*.local
.secrets-backup.txt

# Auto generated files
.DS_Store
Thumbs.db

# Config files on ide and AI
.mcp.json
.vscode/
.idea/
.serverless/
.claude/
.playwright-mcp/
**/CLAUDE.md

# Archivos de TypeScript
*.tsbuildinfo

# Directorios de cobertura de tests (si usas alguna herramienta de coverage)
coverage/

# DB
DML.*

# Terraform
**/.terraform/
*.tfstate
*.tfstate.*
*.tfplan
tfplan
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
*.auto.tfvars
secrets.auto.tfvars
*.secret.tfvars
# Real tfvars are ignored; commit only the *.example templates
*.tfvars
!*.tfvars.example
# Keep .terraform.lock.hcl committed (do NOT ignore it)

# Terraform-loaded secret material (never commit)
infrastructure/secrets/*
!infrastructure/secrets/.gitkeep

# Service account / credential files
*-credentials.json
service-account*.json
