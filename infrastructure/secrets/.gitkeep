# This directory holds LOCAL, GITIGNORED secret material used to push secret versions
# to Secret Manager (e.g. staging.env). Everything here except this .gitkeep is ignored
# by Git (see root .gitignore). NEVER commit real secret values.
