# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Envs
.env
# TanStack Router build artifacts
.tanstack

# `pnpm doc:preview` output — the bundled preview script and the PDFs it renders. A local design
# tool for looking at the order document; nothing here is source or ships anywhere.
scripts/.out
scripts/*.pdf

# Playwright MCP scratch output (screenshots taken while checking a render or a CSP policy)
.playwright-mcp
